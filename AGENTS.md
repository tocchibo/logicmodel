# AGENTS.md

## プロジェクト概要

このリポジトリは、ロジックモデルを描画・編集するための静的Webページです。GitHub Pagesでの公開を前提に開発します。公開物はHTML、CSS、JavaScript、画像など、ブラウザが静的に読み込めるファイルだけにします。

本番用のバックエンドサーバーはありません。Pythonのローカルサーバーは、開発中に静的ファイルを配信するためだけに使います。

## Python と uv のルール

- このリポジトリでPythonを使う場合は、必ず `uv` 経由で実行します。
- Python環境は、uvが作成するプロジェクト直下の `.venv` に隔離します。
- グローバル環境には一切手を付けません。
- グローバルへのパッケージインストールは禁止です。
- このプロジェクトで、グローバルの `pip`、`python -m pip`、システムPythonのパッケージ変更を行ってはいけません。
- `.venv/` はコミットしません。既に `.gitignore` で除外されています。

よく使うコマンド:

```bat
uv sync
uv run python -m http.server 8000
start_server.bat
```

`start_server.bat` は、グローバルの `python` ではなく、必ず `uv run` 経由でローカルサーバーを起動します。

## 静的サイトとしての制約

- GitHub Pagesで動くように、パスは `css/style.css`、`js/app.js`、`pics/example.png` のような相対パスを優先します。
- デプロイ後の動作に、サーバーサイドルーティング、データベース、秘密情報、環境依存のランタイム設定、APIを必須にしてはいけません。
- 明示的に依頼されない限り、ビルドステップは追加しません。標準の開発フローは、静的ファイルサーバー経由で `index.html` を開けば動く状態を保ちます。
- 既存設計に含まれるCDNのブラウザライブラリは利用可能ですが、公開ページは静的サイトである必要があります。

## コード構成

- `index.html` がエントリーポイントです。
- `css/style.css` がアプリケーションのスタイルを持ちます。
- `js/app.js` は `AppState` による中央状態管理を担当します。
- `js/logicModel.js` はコマンド解析とGraphviz DOTへの変換を担当します。
- `js/ui.js` はSVG上の操作、編集パネル、関係線操作を担当します。
- `js/commandsGenerator.js` は内部モデルからコマンド文字列への逆変換を担当します。
- `js/downloads.js` はSVG/PNGエクスポートを担当します。
- `js/constants.js` と `js/utils.js` は共通定数とヘルパーを持ちます。

## 開発時の注意

- 意図的なリファクタリングでない限り、現在のスクリプト読み込み順を維持します。
- 現在のモデルの真実の源は `AppState.logicModelData` として扱います。
- 変更は依頼範囲に絞り、無関係なリファクタリングは避けます。
- ローカルツールを追加する場合も、必ずプロジェクト内に閉じ、`uv` 経由での実行方法を記録します。
